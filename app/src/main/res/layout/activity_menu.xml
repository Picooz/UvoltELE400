<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="com.uvoltappoasis.uvoltappoasis.Page_charge">

    <Button
        android:text="Réglage de l'heure"
        android:layout_height="50dp"
        android:id="@+id/menu_heure"
        android:layout_marginTop="11dp"
        android:textAlignment="center"
        android:onClick="ButtonVersHeure"
        android:layout_width="1000dp"
        android:layout_below="@+id/menu_charge"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:text="Charge de la montre"
        android:layout_width="1000dp"
        android:layout_height="50dp"
        android:id="@+id/menu_charge"
        android:textAlignment="center"
        android:onClick="ButtonVersCharge"
        android:layout_marginTop="179dp"
        android:layout_below="@+id/textView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/bluetooth"
        android:layout_width="1000dp"
        android:layout_height="50dp"
        android:layout_below="@+id/menu_heure"
        android:onClick="ButtonVersBluetooth"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="28dp"
        android:text="BLUETOOTH CONNECT" />

</RelativeLayout>